{
  "name": "external-agent-orchestrator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local MCP worker broker for Claude Code and Antigravity CLI.",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "start": "node scripts/server.mjs"
  },
  "engines": {
    "node": ">=18.18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.29.0"
  }
}
