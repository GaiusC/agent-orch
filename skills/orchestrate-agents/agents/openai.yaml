interface:
  display_name: "Agent Orch"
  short_description: "Delegate code to CC and AGY with auto routing"
  default_prompt: "Use $orchestrate-agents to plan this project, delegate implementation via the auto router, and verify the result through the Agent Orch CLI."

policy:
  allow_implicit_invocation: true
